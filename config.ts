type Config = {  apiUrl: string;  timeout: number;};  class ConfigManager {  private config: Config;  constructor(config: Config) {    this.config = config;  }  getApiUrl(): string {    if (!this.config.apiUrl) {      throw new Error('API URL is not defined');    }    return this.config.apiUrl;  }  getTimeout(): number {    if (this.config.timeout <= 0) {      throw new Error('Timeout must be a positive number');    }    return this.config.timeout;  }}export const configManager = new ConfigManager({  apiUrl: 'https://api.example.com',  timeout: 5000});